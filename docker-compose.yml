services:
  db:
    image: pgvector/pgvector:pg16
    container_name: nano_brain_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 081842000tT
      POSTGRES_DB: nano_brain
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: